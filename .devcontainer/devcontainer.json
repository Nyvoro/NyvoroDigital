// .devcontainer/devcontainer.json
{
  "name": "nyvorodigital",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [3000],
  "postCreateCommand": "pnpm install && npx playwright install-deps",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  }
}
